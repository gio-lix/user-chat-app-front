import React from 'react';
import s from "../../styles/pages/NotFond.module.scss"

const NotFound = () => {
    return (
        <section className={s.root}>
            <h1>Page Not Found.</h1>
        </section>
    );
};

export default NotFound;